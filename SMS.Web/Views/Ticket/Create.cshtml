@model TicketViewModel

<h3 class="mt-4">Create Ticket</h3>

<!-- The form posts back to the TicketController Create POST action --> 

<form asp-action="Create">
     <div class="card rounded shadow p-3">

        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

       
     <!-- lecture code -->    
     <!--<div class="mt-3">
       @* <!-- lecture code --> 
            <label asp-for="StudentId" class="form-label"></label>
            <select asp-for="StudentId" class="form-control" asp-items="@Model.Students"))">
             <option disable selected>Select Student</option>
             </select>
            <span asp-validation-for="StudentId" class="text-danger"></span>
        </div>*@
        <div class="mt-3">
        <!-- My code -->   
        <label asp-for="StudentId" class="form-label"></label>
         <select asp-for="StudentId" class="form-control" asp-items="@Model.Students">
             <option>Select Student</option>
         </select>

        @* <!-- tried alternative code -->
         @Html.LabelFor(m => m.StudentId)
         @Html.DropDownListFor(m => m.StudentId, Model.Students)*@

        <span asp-validation-for="StudentId" class="text-danger"></span>
        </div>

              <div class="mt-3">
            <label asp-for="Issue" class="form-label"></label>
           <textarea rows="5" asp-for="Issue" class="form-control"></textarea>
            <span asp-validation-for="Issue" class="text-danger"></span>
        </div>
  
        <div class="mt-3">
            
            <input type="submit" value="Create" class="btn btn-primary"/>
            <a class="btn btn-secondary" asp-action="Index">Cancel</a>
        </div>
        </div>

    </form>

  <!-- TBC - Add a submit input to post the form and a cancel anchor tag to cancel the form -->
    


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}